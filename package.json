{
  "name": "local-optics",
  "version": "1.0.0",
  "description": "Local Optics design system and components",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 3000",
    "test": "echo \"No tests configured\" && exit 0"
  }
}
